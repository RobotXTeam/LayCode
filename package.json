{
  "name": "layrr-monorepo",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "desktop:dev": "pnpm --filter @laycode/desktop dev",
    "desktop:start": "pnpm --filter @laycode/desktop start",
    "beta:install": "bash scripts/install-beta-launcher.sh"
  },
  "packageManager": "pnpm@10.28.2",
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
